services:
  - type: web
    name: ai-teacher-backend
    env: node
    rootDir: backend
    buildCommand: bash -c "npm install && npm run build"
    startCommand: npm run start:prod
    envVars:
      - key: NODE_VERSION
        value: 20
